[project]
name = "fastapi-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi-mctools>=0.6.0",
    "fastapi[standard]>=0.121.1",
    "gunicorn>=23.0.0",
]
